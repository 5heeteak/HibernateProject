<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.newlecture.webprj.dao.NoteDao">
	
	<!-- <select id="getList"  resultType="com.newlecture.webprj.entity.Note">
		select * from Note
		limit ${(page-1)*15},15
	</select> -->
	
	<!-- <select id="get" resultType="com.newlecture.webprj.entity.Note">
		select * from Note where id=#{id}
	</select>
	
	<insert id="insert" parameterType="com.newlecture.webprj.entity.Note">
		insert into Note(title, content, writerId)
		values(#{title},#{content},#{writerId})
	</insert> -->
	
</mapper>